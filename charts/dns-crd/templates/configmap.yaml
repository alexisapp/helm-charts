apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.dns-crd.config.name }}
  namespace: {{ .Values.dns-crd.config.namespace }}
data:
  {{ range $key, $value := .Values.dns-crd.config.data }}
  {{ $key }}: |
{{ $value | indent 4 }}
  {{ end }}
{{ end }}
